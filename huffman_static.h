#ifndef HUFFMAN_HUFFMAN_STATIC_H
#define HUFFMAN_HUFFMAN_STATIC_H

#include <string>
using namespace std;

int compressStatic(string input, string output, bool model);
int decompressStatic(string, string, bool);

#endif //HUFFMAN_HUFFMAN_STATIC_H
